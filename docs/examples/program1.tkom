int abc = 100;

int sq(int a, int b) {
    return a * a + b * b;
}

void main() {
    int x = 1;
    int y = 2;

    while (x + y <= abc) {
        print(sq(x, y) as string);
        x = x + 2;
        y = y * y;
    }
}